services:
  node:
    build:
      context: ./node
    container_name: node-app
    ports:
      - "3000:3000"
    env_file:
      - .env

  python:
    build:
      context: ./python
    container_name: python-app
    ports:
      - "8000:8000"
    env_file:
      - .env